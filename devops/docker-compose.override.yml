version: "3"

services:
    service_app_weight:
        image: image_app_weight
        volumes:
            - weights:/in
        ports:
            - 6002:5000

    billingserviceapp:
        image: billingserviceapp
        volumes:
            - bill_rates:/in
        ports:
            - 6001:5000

volumes:
    bill_rates:
    weights:
#    ui:
#        service: UI
#        volumes:
#            - weights
#            - bill_rates
#        ports:
#            - 80:80
